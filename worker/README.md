# belote-worker

A Cloudflare worker for the belote web page.

## Commands

| Command | Purpose |
|---------|---------|
| `pnpm dev` | Local development |
| `pnpm deploy` | Deploy to Cloudflare |
| `pnpm types` | Generate TypeScript types |

Run `pnpm types` after changing bindings in wrangler.jsonc.
